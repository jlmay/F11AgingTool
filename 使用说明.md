# F11-N 底座老化测试工具 — 测试人员使用说明

> 版本 1.0 | 适用设备：F11-N（Android 平板 + 底座）

---

## 目录

1. [概述](#1-概述)
2. [安装与部署](#2-安装与部署)
3. [界面说明](#3-界面说明)
4. [测试前的准备工作](#4-测试前的准备工作)
5. [测试流程](#5-测试流程)
6. [老化模式详解](#6-老化模式详解)
7. [五项测试项说明](#7-五项测试项说明)
8. [日志与统计数据](#8-日志与统计数据)
9. [常见问题](#9-常见问题)
10. [附录：权限说明](#10-附录权限说明)

---

## 1. 概述

本工具用于对 **F11-N 设备底座**进行老化（压力）测试，验证 Android 平板与底座之间各功能的长期稳定性。

**可测功能：**

| # | 测试项 | 测试内容 |
|---|--------|----------|
| ① | 身份证读取 | 读取身份证姓名和号码 |
| ② | 非接触 CPU 卡 | 寻卡 → 复位 → 取随机数 |
| ③ | 接触 CPU 卡 | 复位 → 取随机数 |
| ④ | 以太网 PING | 向目标地址发送 PING 请求 |
| ⑤ | 扫码测试 | 扫码枪扫描二维码并与样本码对比 |

**核心特性：**

- 支持两种老化模式（不断电循环 / 每轮重启）
- 测试项目可自由组合勾选
- 支持断电/重启后自动恢复测试
- 实时日志显示 + 历史日志文件保存
- 测试过程中可暂停/继续

---

## 2. 安装与部署

### 2.1 安装 APK

方式一：使用 ADB 安装（推荐）

```bash
# 连接设备后
./build.sh install
# 或直接
adb install app/build/outputs/apk/debug/app-debug.apk
```

方式二：拷贝 APK 到设备手动安装

> APK 位置：`app/build/outputs/apk/debug/app-debug.apk`

### 2.2 设置开机自启动权限（重要）

**步骤：**

1. 打开系统 **设置 → 应用 → F11AgingTool → 显示在其他应用上层**
2. 开启该权限
   > 若不开启，开机自动恢复测试功能将降级为全屏通知模式

### 2.3 （可选）设置设备管理员（模式 2 必需）

如果使用 **模式 2（每轮重启）**，需要将本应用设置为设备所有者：

```bash
adb shell dpm set-device-owner com.decard.f11aging/.DeviceAdminReceiver
```

> 执行前请确保设备上已移除其他设备所有者应用。如执行失败，可先执行 `adb shell dpm remove-active-admin com.decard.f11aging/.DeviceAdminReceiver` 后再重试。

### 2.4 首次启动

点击桌面上的 **F11AgingTool** 图标启动应用，界面标题栏显示完整应用名称和版本号。

---

## 3. 界面说明

应用界面分为以下几个区域（从上到下）：

```
┌──────────────────────────────────┐
│  标题栏                          │  ← 应用名称 + 运行状态（空闲/测试中/已暂停）
├──────────────────────────────────┤
│  老化模式选择                    │  ← RadioGroup，两种模式二选一
├──────────────────────────────────┤
│  参数设置                        │  ← 总次数、启动延时、PING 地址、PING 次数
├──────────────────────────────────┤
│  测试项目勾选                    │  ← 5 个 CheckBox，可多选
├──────────────────────────────────┤
│  遇错停止                        │  ← 勾选后任一测试项失败即终止
├──────────────────────────────────┤
│  扫码样本区（勾选扫码后出现）    │  ← 样本码输入与确认
├──────────────────────────────────┤
│  控制按钮                        │  ← 开始/暂停/停止 + 查看历史日志
├──────────────────────────────────┤
│  测试统计栏（固定显示）          │  ← 已测 / 总计 / 成功 / 失败（带颜色）
├──────────────────────────────────┤
│  网络信息栏                      │  ← 当前设备 IP 地址（测试中显示）
├──────────────────────────────────┤
│  实时日志区                      │  ← 深色背景，等宽字体，最近 10 轮日志
└──────────────────────────────────┘
```

### 按钮状态说明

| 状态 | "开始/暂停测试"按钮 | "停止测试"按钮 |
|------|-------------------|---------------|
| 空闲 | 蓝色「开始测试」 | 禁用 |
| 测试中 | 蓝色「暂停测试」 | 可用（红色） |
| 暂停中 | 琥珀色「继续测试」 | 可用（红色） |

---

## 4. 测试前的准备工作

### 4.1 硬件连接

| 测试项 | 前置条件 |
|--------|---------|
| ① 身份证读取 | 底座 USB 连接正常，读卡器已供电 |
| ② 非接触 CPU 卡 | 底座 USB 连接正常，读卡器已供电 |
| ③ 接触 CPU 卡 | 底座 USB 连接正常，读卡器已供电 |
| ④ 以太网 PING | 底座 USB 网口已连网线，平板自动获取 IP |
| ⑤ 扫码测试 | 底座扫码头已通电，准备一张二维码 |

### 4.2 参数设置

| 参数 | 说明 | 默认值 |
|------|------|--------|
| **测试总次数** | 老化测试的循环次数 | 100 |
| **启动延时** | 点击开始后延迟多久执行（毫秒） | 1000 |
| **PING 地址** | PING 测试的目标服务器地址 | www.baidu.com |
| **PING 次数** | 每轮 PING 测试发送的请求数 | 10 |

### 4.3 样本二维码设置（勾选扫码测试时必需）

**操作步骤：**

1. 勾选「⑤ 扫码测试」→ 自动出现样本码输入框
2. 用扫码枪扫描一张待测的二维码 → 数据自动填入编辑框
3. 系统弹窗显示扫码内容，点击 **「确认」** 锁定样本码
   > 若点击 "取消" 可重新扫描
4. 样本码锁定的编辑框变为只读，显示为灰色底色

> ⚠️ 注意：样本码未确认时无法开始测试。若测试运行中取消样本码，测试将被终止。

---

## 5. 测试流程

### 5.1 基本操作流程

```
① 选择老化模式 → ② 设置参数 → ③ 勾选测试项 
→ ④ 设置扫码样本（如勾选） → ⑤ 点击「开始测试」
```

### 5.2 测试中操作

| 操作 | 说明 |
|------|------|
| **暂停测试** | 点击「暂停测试」，当前测试项完成后暂停，可查看实时日志 |
| **继续测试** | 点击「继续测试」，从暂停处恢复 |
| **停止测试** | 随时点击红色「停止测试」，立即终止，清除自动恢复标志 |

> ⚠️ 点击停止后，测试**不能恢复**，需要重新配置并手动启动。

### 5.3 断电/重启后自动恢复

一旦测试开始，系统会自动保存当前测试状态。以下情况会自动恢复：

1. **意外断电**：重新上电开机后，应用自动检测到未完成的测试，按照配置继续执行
2. **模式 2 重启**：系统重启后自动恢复，进入下一轮测试
3. **手动重启**：同样会触发自动恢复

> 恢复时，屏幕会显示 `[恢复] 开机检测到未完成测试，模式=xxx，剩余 X/Y 次`

---

## 6. 老化模式详解

### 模式 1：不断电循环

| 特性 | 说明 |
|------|------|
| 运行方式 | 持续运行所有勾选的测试项，达到设定次数后停止 |
| 断电恢复 | ✅ 支持，开机后自动从断点继续 |
| 适用场景 | 长期稳定性测试，无需重启设备 |

### 模式 2：每轮重启

| 特性 | 说明 |
|------|------|
| 运行方式 | 每执行完一轮测试后自动重启系统，开机后自动进行下一轮 |
| 断电恢复 | ✅ 支持 |
| 重启方式 | 依次尝试以下 4 种方式，全部失败则降级为模式 1 |
| 适用场景 | 验证设备在反复重启后的稳定性 |

**重启尝试顺序：**

```
① DevicePolicyManager.reboot()  →  ② su reboot
→ ③ PowerManager.reboot()      →  ④ reboot 命令
→ 全部失败 → 降级为模式 1 继续
```

> 推荐使用方式 ①（需设置设备所有者），方式 ② 需要 root。

---

## 7. 五项测试项说明

### ① 身份证读取

| 项目 | 内容 |
|------|------|
| 成功条件 | 成功读取序列号 → 读取姓名和身份证号码 |
| 屏幕显示 | 序列号、姓名、身份证号 |
| 失败原因 | 读卡器未连接、身份证未放置、读卡器初始化失败 |

> 每轮测试前请确保身份证已放置在读卡器感应区。

### ② 非接触 CPU 卡

| 项目 | 内容 |
|------|------|
| 测试步骤 | ① 配置卡类型 → ② 射频复位 → ③ 寻卡（Type A）→ ④ CPU 卡复位 → ⑤ 取随机数 → ⑥ 下电 |
| 成功条件 | 全部 6 步成功 |
| 失败原因 | 卡片未放置、卡片类型不兼容、读卡器异常 |

> 测试完成后会自动下电（释放卡片），确保下轮寻卡正常。

### ③ 接触 CPU 卡

| 项目 | 内容 |
|------|------|
| 测试步骤 | ① 复位 → ② 取随机数 |
| 成功条件 | 两步均成功 |
| 失败原因 | 卡片未插入、卡片接触不良、读卡器异常 |

### ④ 以太网 PING

| 项目 | 内容 |
|------|------|
| 测试步骤 | 获取设备 IP → 向目标地址发送 PING 请求（可配次数） |
| 成功条件 | 所有 PING 请求均成功 |
| 失败原因 | 网线未连接、DNS 解析失败、网络不通 |
| 可配参数 | 目标地址（默认 `www.baidu.com`）、PING 次数（默认 10 次） |

> 测试开始时底部会显示当前设备的 IP 地址，可判断网络连接状态。

### ⑤ 扫码测试

| 项目 | 内容 |
|------|------|
| 测试步骤 | ① 清空编辑框 → ② 等待扫码数据（最多 10 秒）→ ③ 文本稳定（200ms 无变化）→ ④ 与样本码对比 |
| 成功条件 | 扫码数据与样本码完全一致 |
| 失败原因 | 扫码超时（10 秒无数据）、扫码内容与样本码不一致、未配置样本码 |

**扫码测试工作机制：**

- 每轮开始前清空编辑框、获取焦点（已抑制软键盘弹出）
- 扫码枪通过 HID 键盘模拟输入数据
- 系统检测到文本稳定（连续 50ms 无新字符变化）认为接收完毕
- 若 10 秒内无任何扫码输入，判定超时失败

> ⚠️ 样本码未确认时扫码测试会自动终止整个测试。恢复测试时（模式 2 重启后）不会重新弹出样本确认弹窗，使用已保存的样本码继续。

---

## 8. 日志与统计数据

### 8.1 统计栏（固定显示）

屏幕中部始终显示以下统计数据（粗体大号字体）：

| 项目 | 颜色 | 说明 |
|------|------|------|
| 已测 | 蓝色 | 已完成的测试轮数 |
| 总计 | 深色 | 设定的总测试次数 |
| 成功 | 绿色 | 全部测试项通过的总轮数 |
| 失败 | 红色 | 有测试项失败的总轮数 |

### 8.2 实时日志（屏幕底部）

- 深色背景、等宽字体
- 自动滚动显示最近 **10 轮**的详细日志
- 包含各项测试的调试信息（序列号、ATR、随机数等）
- 点击右上角「清除」按钮可清空

### 8.3 历史日志（文件）

- 日志文件保存路径：`Android/data/com.decard.f11aging/files/logs/` 下的 `aging_yyyyMMdd_HHmmss.txt`
- 同一测试项目重启后日志继续写入同一文件
- 点击 **「查看历史日志」** 按钮可浏览已有日志文件

**日志包含的关键信息：**

```
====== 测试开始 [20260602_093000] ======
总次数=100 | 模式=不断电循环 | 延时=1000ms
测试项目: ①身份证 ④以太网

[2026-06-02 09:30:05] 取序列号失败: xxxx
[2026-06-02 09:30:05] 身份证: ✗ 失败
[2026-06-02 09:30:10] 以太网 PING 超时
[2026-06-02 09:30:10] 以太网PING: ✗ 失败

====== 全部测试完成 ======
成功: 95  失败: 5  总计: 100
```

> **日志记录原则：** 仅记录失败信息和关键事件，不记录成功时的详细调试信息。

**日志操作：** 在「查看历史日志」弹窗中：

- 勾选文件后点击 **「查看」** 浏览文件内容
- 勾选文件后点击 **「删除选中」** 删除不需要的日志文件

---

## 9. 常见问题

### Q1：点击开始测试后没有反应

- 检查是否设置了有效的测试次数（需 > 0）
- 检查是否至少勾选了一个测试项
- 检查扫码模式下是否已确认样本码
- 查看屏幕底部日志是否有错误信息

### Q2：读卡器测试一直失败

- 检查底座 USB 连接是否正常
- 检查读卡器指示灯是否亮起
- 如果是接触式 CPU 卡，确认卡片插入方向正确
- 检查实时日志中读卡器打开的成功/失败信息

### Q3：模式 2 无法自动重启

- 检查是否已设置设备所有者（见 [2.3 节](#23-可选设置设备管理员模式-2-必需)）
- 如果 4 种重启方式都失败，模式 2 会自动降级为模式 1 继续测试（界面会有提示）
- 降级后测试继续运行，不会中断

### Q4：断电后开机没有自动恢复测试

- 检查是否开启了「显示在其他应用上层」权限（见 [2.2 节](#22-设置开机自启动权限重要)）
- 检查应用是否有开机自启动权限（Android 系统设置中）
- 如果持久化数据已清除则无法恢复（可能是测试已完成或手动停止过）

### Q5：扫码测试一直提示超时

- 检查扫码枪是否通电
- 检查扫码枪与设备的连接是否正常
- 测试前确保编辑框已获取焦点（焦点应该在编辑框中）
- 每轮等待扫码数据的时间上限为 10 秒

### Q6：如何判断测试是否在进行中？

- 标题栏右侧状态显示为 **「● 测试中」**（琥珀色）
- 统计栏的数字在不断更新
- 底部实时日志在不断滚动输出

### Q7：如何中途修改测试参数？

测试运行中**不能修改参数**。如需修改：
1. 点击红色「停止测试」按钮停止当前测试
2. 修改参数
3. 重新点击「开始测试」

---

## 10. 附录：权限说明

应用需要以下权限：

| 权限 | 用途 |
|------|------|
| `INTERNET` | 以太网 PING 测试 |
| `ACCESS_NETWORK_STATE` | 获取设备 IP 地址 |
| `RECEIVE_BOOT_COMPLETED` | 开机自启动恢复测试 |
| `FOREGROUND_SERVICE` | 开机自启所需的前台服务 |
| `SYSTEM_ALERT_WINDOW` | Android 10+ 后台启动 Activity（开机自启必需） |
| `USE_FULL_SCREEN_INTENT` | Android 14+ 全屏通知（开机自启降级方案） |
| USB Host 功能 | 连接底座 USB 读卡器 |

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-06-02 | 初始版本 |

---

*如有其他问题，请联系开发人员。*
